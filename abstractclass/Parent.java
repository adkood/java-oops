package oops.abstractclass;

public abstract class Parent {

    // can't create abstract constructor
    // abstract Parent() {} 

    // can't create abstract static method
    // abstract static void career(String choice);

    abstract void career(String choice);
}
